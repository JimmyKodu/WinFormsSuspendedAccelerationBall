# 半透明悬浮球 UI 设计文档

## 悬浮球主界面

### 视觉效果

```
                    ╭──────────────╮
                  ╱                  ╲
                ╱    半透明渐变球      ╲
              ╱       根据CPU负载       ╲
             │        动态变色          │
             │                          │
            │         45%              │
            │         CPU              │
             │                          │
             │      内存: 62%           │
              ╲                        ╱
                ╲                    ╱
                  ╲                ╱
                    ╰──────────────╯
```

### 尺寸规格
- **大小**: 150 x 150 像素
- **形状**: 完美圆形
- **位置**: 默认在屏幕右上角，可自由拖动
- **层级**: 始终置顶 (TopMost = true)

### 颜色方案

根据CPU使用率动态变化：

| CPU 使用率 | 颜色 | 含义 |
|-----------|------|------|
| 0-30% | 🟢 绿色 (76, 175, 80) | 性能良好 |
| 30-60% | 🟡 黄色 (255, 193, 7) | 正常使用 |
| 60-80% | 🟠 橙色 (255, 152, 0) | 较高负载 |
| 80-100% | 🔴 红色 (244, 67, 54) | 高负载警告 |

### 透明度效果
- **中心颜色**: Alpha = 200 (较不透明)
- **边缘颜色**: Alpha = 100 (更透明)
- **外环**: Alpha = 180 (白色边框)

## 交互方式

### 1. 鼠标拖动
- **操作**: 左键按住悬浮球拖动
- **效果**: 可将悬浮球拖动到屏幕任意位置
- **用途**: 避免遮挡工作区域

### 2. 右键菜单
右键点击悬浮球显示快捷菜单：

```
┌────────────────┐
│ 查看详情       │ → 打开详细信息窗口
├────────────────┤
│ 一键优化       │ → 执行系统优化
├────────────────┤
│ 进程管理       │ → 打开进程管理器
├────────────────┤
│ ────────       │
├────────────────┤
│ 退出           │ → 关闭应用程序
└────────────────┘
```

### 3. 双击展开
- **操作**: 双击悬浮球
- **效果**: 打开"系统监控详情"窗口
- **内容**: 完整的系统信息展示

## 详情窗口 (DetailsForm)

双击或右键菜单"查看详情"后显示：

```
┌─────────────────────────────────────┐
│        系统监控详情            [×]  │
├─────────────────────────────────────┤
│  ┌─ CPU占用 ─────────────────┐     │
│  │  45.3%                     │     │
│  │  ████████████░░░░░░░       │     │
│  └────────────────────────────┘     │
│                                      │
│  ┌─ 内存占用 ────────────────┐     │
│  │  62.1% (4,972 / 8,000 MB) │     │
│  │  ████████████████░░░       │     │
│  └────────────────────────────┘     │
│                                      │
│  ┌─ 硬件温度 ────────────────┐     │
│  │  CPU: 52.3°C               │     │
│  └────────────────────────────┘     │
│                                      │
│  ┌─ 网络速度 ────────────────┐     │
│  │  下载: 2.35 MB/s           │     │
│  │  上传: 156.8 KB/s          │     │
│  └────────────────────────────┘     │
│                                      │
│  [  一键优化  ]  [ 进程管理 ]       │
└─────────────────────────────────────┘
```

### 详情窗口特性
- **尺寸**: 394 x 584 像素
- **位置**: 屏幕中央
- **更新**: 每秒自动刷新
- **功能**: 显示完整的系统信息和快捷操作按钮

## 进程管理窗口 (ProcessManagerForm)

点击"进程管理"后显示：

```
┌──────────────────────────────────────────────────────────┐
│              进程管理                              [×]  │
├──────────────────────────────────────────────────────────┤
│ 进程分类: [全部 ▼]  [调整优先级] [结束进程] [刷新]     │
├──────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────┐  │
│ │进程ID│进程名称    │分类  │内存(MB)│CPU(%)│线程数││  │
│ ├────────────────────────────────────────────────────┤  │
│ │1234  │chrome.exe  │浏览器│  1,234 │ 15.2 │  45  ││  │
│ │5678  │devenv.exe  │开发  │  2,456 │ 25.8 │  89  ││  │
│ │9012  │explorer.exe│系统  │    345 │  5.1 │  23  ││  │
│ │...   │...         │...   │    ... │ ...  │ ... ││  │
│ └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 进程管理特性
- **尺寸**: 900 x 540 像素
- **更新**: 每2秒自动刷新
- **筛选**: 按7种类别筛选进程
- **操作**: 结束进程、调整优先级

## 技术实现细节

### 1. 圆形窗口实现
```csharp
// 创建圆形区域
GraphicsPath path = new GraphicsPath();
path.AddEllipse(0, 0, BallSize, BallSize);
this.Region = new Region(path);
```

### 2. 半透明效果
```csharp
// 使用透明键实现半透明
this.BackColor = Color.Lime;
this.TransparencyKey = Color.Lime;

// PathGradientBrush 创建渐变效果
PathGradientBrush brush = new PathGradientBrush(path);
brush.CenterColor = Color.FromArgb(200, color);
brush.SurroundColors = new Color[] { Color.FromArgb(100, color) };
```

### 3. 拖动功能
```csharp
// 鼠标事件处理
protected override void OnMouseDown(MouseEventArgs e)
{
    if (e.Button == MouseButtons.Left)
    {
        isDragging = true;
        lastPoint = e.Location;
    }
}

protected override void OnMouseMove(MouseEventArgs e)
{
    if (isDragging)
    {
        this.Location = new Point(
            this.Location.X + e.X - lastPoint.X,
            this.Location.Y + e.Y - lastPoint.Y);
    }
}
```

### 4. 动态颜色变化
```csharp
private Color GetColorForUsage(float usage)
{
    if (usage < 30) return Color.FromArgb(76, 175, 80);   // Green
    if (usage < 60) return Color.FromArgb(255, 193, 7);   // Yellow
    if (usage < 80) return Color.FromArgb(255, 152, 0);   // Orange
    return Color.FromArgb(244, 67, 54);                   // Red
}
```

## 使用场景

### 1. 日常监控
- 悬浮球常驻桌面右上角
- 实时查看CPU和内存使用情况
- 根据颜色快速判断系统状态

### 2. 性能优化
- 发现系统变慢时
- 右键点击"一键优化"
- 快速释放内存和优化进程

### 3. 进程管理
- 需要查看或管理进程时
- 右键点击"进程管理"
- 查看、结束或调整进程优先级

### 4. 详细诊断
- 需要详细系统信息时
- 双击悬浮球
- 查看完整的性能指标

## 优势特点

✨ **简洁美观**: 圆形悬浮球设计，不占用过多屏幕空间
🎨 **视觉反馈**: 根据CPU负载动态变色，一目了然
🖱️ **灵活操作**: 可拖动、右键菜单、双击展开
⚡ **性能优化**: 最小化资源占用，不影响系统性能
🔄 **实时更新**: 每秒刷新数据，保持信息最新
🛡️ **安全可靠**: 保护系统关键进程，安全优化

## 注意事项

1. **硬件要求**: 建议支持透明效果的显示器和显卡
2. **系统兼容**: Windows 10 或更高版本
3. **权限需求**: 某些功能需要管理员权限
4. **资源占用**: 悬浮球本身占用约 20-30 MB 内存

## 未来增强

- [ ] 自定义悬浮球大小
- [ ] 更多颜色主题选择
- [ ] 可配置显示信息（CPU、内存、网络等）
- [ ] 支持多显示器定位
- [ ] 添加快捷键支持
- [ ] 保存窗口位置偏好
