# 系统性能监控与优化工具 - 使用指南

## 概述

本应用程序是一个功能强大的 Windows 系统监控和优化工具，提供实时性能监控和进程管理功能。

## 主要功能

### 1. 系统监控面板

#### CPU 监控
- 实时显示 CPU 使用率百分比
- 可视化进度条显示
- 每秒更新一次数据

#### 内存监控
- 显示当前内存使用百分比
- 显示已用内存和总内存（MB）
- 可视化进度条显示
- 帮助识别内存瓶颈

#### 硬件温度
- 监控 CPU 温度（摄氏度）
- 需要硬件传感器支持
- 如果不可用会显示 "不可用"

#### 网络速度
- 实时显示下载速度
- 实时显示上传速度
- 自动格式化显示单位（B/s, KB/s, MB/s）

#### 一键优化
- 点击"一键优化"按钮执行系统优化
- 自动清理内存
- 优化高资源占用进程
- 显示优化结果统计

### 2. 进程监控面板

#### 进程列表
显示所有运行中进程的详细信息：
- **进程 ID (PID)**: 唯一标识符
- **进程名称**: 程序名称
- **分类**: 自动分类（见下文）
- **内存占用**: 以 MB 为单位
- **CPU 使用率**: 百分比
- **线程数**: 进程线程数量

#### 智能分类
进程自动分为以下类别：
- **系统进程**: Windows 核心服务和系统进程
- **浏览器**: Chrome, Firefox, Edge 等浏览器
- **开发工具**: Visual Studio, VS Code, Git 等
- **媒体应用**: 音视频播放器
- **办公软件**: Office 套件
- **游戏**: 游戏应用
- **其他应用**: 未分类的应用程序

#### 进程管理功能

##### 1. 筛选进程
- 使用"进程分类"下拉菜单
- 选择"全部"查看所有进程
- 选择特定类别仅显示该类别的进程

##### 2. 刷新列表
- 点击"刷新"按钮手动更新进程列表
- 进程列表在查看该标签页时自动更新

##### 3. 结束进程
- 选择要结束的进程
- 点击"结束进程"按钮
- 确认后进程将被终止
- **警告**: 结束系统进程可能导致系统不稳定

##### 4. 调整优先级
- 选择要调整的进程
- 点击"调整优先级"按钮
- 从弹出对话框中选择优先级：
  - 实时 (最高)
  - 高
  - 高于正常
  - 正常 (默认)
  - 低于正常
  - 低 (最低)
- **注意**: 某些系统进程可能无法调整优先级

## 系统要求

### 必需
- Windows 10 或更高版本
- .NET 6.0 Runtime 或更高版本

### 可选
- 管理员权限（用于完整功能访问）
- 硬件传感器支持（用于温度监控）

## 安装和运行

### 从源代码构建

```bash
# 克隆仓库
git clone https://github.com/JimmyKodu/WinFormsSuspendedAccelerationBall.git
cd WinFormsSuspendedAccelerationBall

# 构建项目
dotnet build SystemMonitor.sln

# 运行应用程序
dotnet run --project SystemMonitor/SystemMonitor.csproj
```

### 发布可执行文件

```bash
# 发布为单文件可执行程序
dotnet publish SystemMonitor/SystemMonitor.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true

# 可执行文件位于
# SystemMonitor/bin/Release/net6.0-windows/win-x64/publish/SystemMonitor.exe
```

## 使用技巧

### 最佳实践

1. **以管理员身份运行**
   - 右键点击应用程序
   - 选择"以管理员身份运行"
   - 这允许访问所有系统功能

2. **定期监控**
   - 保持应用程序在后台运行
   - 定期检查系统性能指标
   - 在系统变慢时使用一键优化

3. **进程管理**
   - 按内存占用排序找出资源消耗大的应用
   - 使用分类功能快速定位特定类型的进程
   - 谨慎结束进程，避免影响系统稳定性

### 故障排除

#### 温度显示"不可用"
- 这是正常的，不是所有硬件都支持温度传感器
- 某些系统需要特定驱动程序
- 功能性不受影响

#### 无法结束某些进程
- 某些系统进程受保护
- 以管理员身份运行应用程序
- 某些进程需要特殊权限

#### CPU/内存数据不准确
- 等待几秒让计数器初始化
- Performance Counter 需要时间稳定
- 刷新应用程序重新初始化

## 安全注意事项

1. **系统进程**: 不要结束标记为"系统进程"的进程
2. **备份**: 在进行重大更改前备份重要数据
3. **管理员权限**: 仅在需要时使用管理员权限
4. **优化功能**: 一键优化是安全的，只降低非关键进程的优先级

## 技术说明

### 使用的 API
- **Performance Counters**: CPU、内存、网络监控
- **System.Management (WMI)**: 硬件信息查询
- **Process API**: 进程管理和监控

### 性能影响
- 应用程序本身占用最小资源
- 更新间隔: 1 秒
- 不会显著影响系统性能

### 数据隐私
- 所有数据仅在本地处理
- 不收集或传输任何数据
- 不进行网络通信（除本地系统监控外）

## 许可证

本项目采用开源许可证。详见 LICENSE 文件。

## 支持和贡献

如有问题或建议，请在 GitHub 上提交 Issue：
https://github.com/JimmyKodu/WinFormsSuspendedAccelerationBall/issues

欢迎贡献代码！请提交 Pull Request。

## 版本历史

### v1.0.0 (当前版本)
- 初始版本
- 实时系统监控（CPU、内存、温度、网络）
- 进程监控和管理
- 智能进程分类
- 一键优化功能
